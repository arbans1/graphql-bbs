query User($id: ID!) {
    user(id: $id) {
        id
        email
        profile {
            nickname
            imageUrl
        }
        role
        status
        createdAt
        lastLoginAt
    }
}
